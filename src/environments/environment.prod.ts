/**
 * Variables de entorno para PRODUCCIÓN.
 * Reemplaza a environment.ts al hacer `ng build` (configurado en angular.json).
 */
export const environment = {
  production: true,
  apiUrl: '/api',
};
